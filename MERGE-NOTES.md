# 三版本整併歷史封存

本檔僅記錄 2026 年單一 PWA 整併前的歷史脈絡，不是現行操作指引。

過去專案曾同時維護桌機雜誌版、手機旅伴版與 iOS App 預覽版；整併後，根網址 `/` 的 `B-companion` 是唯一正式產品。`mobile.html`、`desktop.html`、`app-preview.html` 只保留既有書籤相容轉址，A／C 介面只作設計研究留存。

現行架構、維護方式與驗收指令以 `README.md`、單一 PWA 設計規格及 `./verify.sh` 為準。
